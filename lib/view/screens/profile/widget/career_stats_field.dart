import 'package:flutter/material.dart';

class CareerStatsField extends StatelessWidget {
  const CareerStatsField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
